# Email credentials are automatically read from ../MailAPI/config/.env
# No need to duplicate EMAIL_ADDRESS and EMAIL_PASSWORD here

# MineContext Web API Configuration
MINECONTEXT_API_URL=http://localhost:8765
MINECONTEXT_AUTH_TOKEN=default_token

# Optionally override recipient email
# RECIPIENT_EMAIL=student@example.com
